<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 ">
        <h5>You Can Edit Post.</h5>
        <form method="post" action="/update/<%= post._id%>" >
            <textarea placeholder="Whats's On Your Mind ?" class="text-zinc-400 block w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md mt-5 p-2 outline-none " name="content" ><%= post.content %></textarea>
            <input type="submit" value="Update Post" class="block bg-yellow-500 text-black px-2 text-xl rounded-md mt-2 cursor-pointer	" >
        </form>
    </div>
</body>
</html>