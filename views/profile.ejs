<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 ">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Log Out</a>
        </div>
        <h3 class="text-3xl mb-5">Hello, <%= user.name %> 👋🏻 </h3>
        <h5>You Can Create A New Post.</h5>
        <form method="post" action="/post" class="text-white">
            <textarea placeholder="Whats's On Your Mind ?" class=" block w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md mt-5 p-2 outline-none" name="content" ></textarea>
            <input type="submit" value="Create New Post" class="block bg-blue-500 px-2 text-xl rounded-md mt-2 cursor-pointer	" >
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/2 p-5 mb-4 border-2 border-zinc-800 bg-zinc-800 rounded-md">
                        <h4 class="text-blue-500">@<%=user.username %> </h4>
                        <p class="mt-2 text-sm tracking-tighter"><%= post.content%></p>
                        <small class="mt-2 inline-block"><%= post.likes.length%> Likes</small>
                        <div class="btns mt-5 flex gap-5">
                            <a class="text-blue-500" href="/like/<%= post._id%>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%= post._id%>">Edit</a>
                        </div>
                    </div>
                    <%})%>
            </div>

        </div>






    </div>
</body>
</html>